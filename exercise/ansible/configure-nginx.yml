- hosts: [server]

  roles:
    - role: install-nginx

  tasks:
    - name: Configure my own page
      become: yes
      copy:
        src: ../assets/index.html
        dest: /var/www/html/index.nginx-debian.html
